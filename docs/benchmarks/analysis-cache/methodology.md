# Methodology

## Environment

- Benchmark started: 2026-08-29T12:24:56.061Z
- Benchmark ended: 2026-08-29T13:10:02.137Z
- Node.js: v22.16.0
- OS: Windows_NT 10.0.19045 x64
- CPU: Intel(R) Core(TM) i5-4690 CPU @ 3.50GHz (4 logical CPUs)
- Memory: 15.9 GB
- Cogic commit SHA: d9faa0221760f6d1b345ae067a3b37362562e1a0

## Measurement Settings

- Warm-up runs per condition: 5
- Measured runs per condition: 30
- Timer: `performance.now()`
- Graph depth: 1
- Trace mode: false
- Trace scope: `single-file`
- Workspace file cap: 4000
- UI rendering: excluded
- 350 ms document-change debounce: excluded
- VS Code `findFiles` workspace discovery: excluded from timing by fixing the file path list before analysis measurement

The measured interval is the workspace graph analysis execution path, including analyzer work and the benchmark's panel-equivalent result-cache lookup/store when that mode is enabled.

## Cache Modes

- `OFF`: analyzer disk `SourceFile` cache disabled; full analysis result cache disabled.
- `SourceFile only`: analyzer disk `SourceFile` cache enabled; full analysis result cache disabled.
- `Full result`: analyzer disk `SourceFile` cache enabled; benchmark harness uses panel-equivalent full-result keying, JSON clone, LRU pruning, and return-on-hit behavior.

The user-facing `cogic.analysisCache.enabled` setting controls both the panel-level full analysis result cache and the analyzer disk `SourceFile` cache. The workspace file list cache is separate and short-lived in the VS Code panel layer.

## Workloads

| Target | Type | Files | Active file | Commit | tsconfig | Notes |
|---|---|---:|---|---|---:|---|
| Cogic repository | project | 84 | `src/panel/CodeGraphPanel.ts` | d9faa0221760f6d1b345ae067a3b37362562e1a0 | yes | Cogic repository itself. |
| Synthetic TypeScript workspace (100 files) | synthetic | 100 | `entry.ts` | d9faa0221760f6d1b345ae067a3b37362562e1a0 | yes | Generated by `scripts/benchmark-analysis-cache.mjs`. |
| Synthetic TypeScript workspace (500 files) | synthetic | 500 | `entry.ts` | d9faa0221760f6d1b345ae067a3b37362562e1a0 | yes | Generated by `scripts/benchmark-analysis-cache.mjs`. |
| Synthetic TypeScript workspace (1000 files) | synthetic | 1000 | `entry.ts` | d9faa0221760f6d1b345ae067a3b37362562e1a0 | yes | Generated by `scripts/benchmark-analysis-cache.mjs`. |
| Synthetic TypeScript workspace (2000 files) | synthetic | 2000 | `entry.ts` | d9faa0221760f6d1b345ae067a3b37362562e1a0 | yes | Generated by `scripts/benchmark-analysis-cache.mjs`. |
| External real-world repository (zod) | external real-world | 490 | `packages/zod/src/index.ts` | e6b6ab347675cd2bd54b1bdbed16f98c59be82a9 | yes | https://github.com/colinhacks/zod.git; A public, real-world TypeScript library that is large enough to exercise workspace analysis but not an oversized monorepo. |

Synthetic workspaces are generated deterministically. Their dependency shape is `entry.ts -> service-N.ts -> helper-N.ts -> utility-N.ts`; generated files include import/export statements, function calls, interfaces/types, and multi-level dependencies.

## Scenarios

1. Cold Analysis: all relevant benchmark/analyzer caches are cleared before every iteration. This measures first-run/cache-creation overhead, not cache hits.
2. Repeated Identical Analysis: active code, graph depth, trace settings, and workspace file state stay identical. This is the best-case path for the full analysis result cache.
3. Active-file Edit Analysis: active in-memory code changes every iteration. The final result cache key changes, so full-result cache hits are intentionally prevented; unchanged dependency `SourceFile` reuse can still help.

Active-file edit analysis is closest to a normal editing workflow among these scenarios, because the active buffer changes while most dependency files remain unchanged.

## Correctness Validation

For each target, cache OFF was compared with `SourceFile only` and `Full result` using a normalized analysis result. The normalized comparison sorts graph nodes, graph edges, imports, exports, calls, and diagnostics, then hashes the stable representation. The report records node count, edge count, import count, export count, call count, diagnostic count, and whether the normalized graph hash matched.

